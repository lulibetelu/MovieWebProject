<!DOCTYPE html>
<html>
<head>
    <title>Películas con <%= personData.actor_name %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
<h1><%= personData.actor_name%></h1>
<h2>Peliculas</h2>
<ul>
    <% personData.movies.sort((a, b) => new Date(b.release_date) - new Date(a.release_date)).forEach((movie) => { %>
        <li><a href="/pelicula/<%= movie.movie_id %>"><%= movie.title %> (<%= new Date(movie.release_date).getFullYear() %>)</a></li>
    <% }); %>
</ul>
<h2>Información Personal</h2>
<ul>
    <li>Nombre Completo: <%= personData.actor_name%></li>
    <li>Género: <%= personData.gender%></li>
</ul>
<p></p>
<div id="inicio"><a href="/">Inicio</a></div>
</body>
</html>


